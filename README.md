# Modern.js App

## Setup

Install the dependencies:

```bash
yarn install
```

Build the app for production:

```bash
yarn build
```

Preview the production build locally:

```bash
yarn serve
```

Deploy

```bash
npx modern deploy
```

```
node .output/index
```
